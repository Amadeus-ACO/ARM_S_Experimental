package  entity.work.task;

import org.springframework.data.jpa.repository.JpaRepository;

import java.util.UUID;

public interface TaskSectionTreeRepository extends JpaRepository<TaskSectionTree, UUID> {
}
